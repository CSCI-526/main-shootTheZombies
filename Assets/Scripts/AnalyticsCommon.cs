using UnityEngine;

public class AnalyticsCommon : MonoBehaviour
{
    public static long timeZombieKilled { get; set; }

    //public static long TimeZombieKiled
    //{
    //    get { return timeZombieKilled; }
    //    set { timeZombieKilled = value; }
    //}

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
